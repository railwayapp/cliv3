use super::*;

/// List all projects in your Railway account
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("list command is not implemented yet");
    Ok(())
}
