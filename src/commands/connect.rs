use super::*;

/// Open an interactive shell to a database
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("connect command is not implemented yet");
    Ok(())
}
