use super::*;

/// Disassociate project from current directory
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("unlink command is not implemented yet");
    Ok(())
}
