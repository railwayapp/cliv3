use super::*;

/// Upload and deploy project from the current directory
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("up command is not implemented yet");
    Ok(())
}
