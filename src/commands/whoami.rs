use super::*;

/// Get the current logged in user
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("whoami command is not implemented yet");
    Ok(())
}
