use super::*;

/// Change the active environment
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("environment command is not implemented yet");
    Ok(())
}
