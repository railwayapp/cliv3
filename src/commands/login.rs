use super::*;

/// Login to your Railway account
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("login command is not implemented yet");
    Ok(())
}
