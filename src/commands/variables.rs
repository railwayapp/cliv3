use super::*;

/// Show variables for active environment
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("variables command is not implemented yet");
    Ok(())
}
