use super::*;

/// [EXPERIMENTAL!] Protect current branch (Actions will require confirmation)
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("protect command is not implemented yet");
    Ok(())
}
