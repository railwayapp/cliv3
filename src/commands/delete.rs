use super::*;

/// Delete Project, may specify projectId as an argument
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("delete command is not implemented yet");
    Ok(())
}
