use super::*;

/// Remove the most recent deployment
#[derive(Parser)]
pub struct Args {}

pub async fn command(args: Args) -> Result<()> {
    unimplemented!("down command is not implemented yet");
    Ok(())
}
